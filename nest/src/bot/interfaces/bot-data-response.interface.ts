export default interface BotDataResponse {
    /**
     * The discord tag that the this function returns
     */
    tag: string;
    /**
     * The URL to the discord avatar that this function returns.
     */
    avatarUrl: string;
}